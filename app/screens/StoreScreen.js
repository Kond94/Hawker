import React, { useEffect, useState } from "react";

import AppText from "../components/Text";
import { StyleSheet } from "react-native";

const StoreScreen = ({ navigation }) => {
  useEffect(() => {}, []);
  return <AppText>Stores</AppText>;
};

export default StoreScreen;
